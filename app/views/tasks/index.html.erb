<% if logged_in? %>
    <div class="row">
      <div class="col-sm-8">
        <h1>タスク一覧</h1>
        <%= render 'tasks/tasks', tasks: @tasks %>
      </div>
    </div>
<% else %>
  <%= redirect_to login_url %>
<% end %>

